# 03 核心数据契约

## 1. 目标
- 本文档只定义高风险对象的“字段集合和语义”，不绑定具体 TypeScript 文件名。
- 允许字段顺序不同，不允许核心字段缺失或改义。
- 当前代码基准以 `src/shared/types.ts`、`src/shared/orchestration-contracts.ts`、`src/shared/execution-bundle.ts` 为准；本文不得保留与这些共享契约冲突的第二套字段名或枚举值。

## 2. ProjectSummary
```json
{
  "projectId": "string",
  "projectName": "string",
  "projectRootPath": "string",
  "templateId": "string|null",
  "lastOpenedAt": "string",
  "activeFlowId": "string|null"
}
```

## 3. ProviderProfile
```json
{
  "id": "string",
  "name": "string",
  "providerType": "ollama|deepseek|openai-compatible|mock",
  "baseUrl": "string",
  "model": "string",
  "apiKeyRef": "string|null",
  "enabled": true,
  "createdAt": "string",
  "updatedAt": "string"
}
```

## 4. RolePackageManifest
```json
{
  "id": "string",
  "name": "string",
  "version": "string",
  "entryFiles": {
    "identity": "IDENTITY.md",
    "soul": "SOUL.md",
    "agents": "AGENTS.md",
    "user": "USER.md",
    "memory": "MEMORY/MEMORY.md"
  },
  "skillsDirectory": "Skills",
  "defaultSkillIds": ["string"],
  "allowedCapabilityIds": ["string"],
  "defaultModelPolicy": {
    "mode": "fixed|prefer_list|capability_match|policy_router|fallback_to_active",
    "fixedProfileId": "string|null",
    "preferredProfileIds": ["string"],
    "fallbackToActive": true
  }
}
```

## 5. FlowDraft
```json
{
  "id": "string",
  "name": "string",
  "description": "string",
  "kind": "flow|subflow",
  "createdAt": "string",
  "updatedAt": "string",
  "pathConfig": "FlowPathConfig|null",
  "roleIds": ["string"],
  "nodes": ["PlatformFlowNode"],
  "edges": ["PlatformFlowEdge"]
}
```

## 6. NodeDraft
```json
{
  "id": "string",
  "type": "start|end|agent|tool|condition|loop|approval|parallel_split|parallel_join|subflow|artifact",
  "position": { "x": 0, "y": 0 },
  "data": {
    "label": "string",
    "description": "string|null",
    "roleId": "string|null",
    "taskTemplateId": "string|null",
    "agentProfileId": "string|null",
    "skillIds": ["string"],
    "connectorId": "string|null",
    "toolId": "string|null",
    "toolIds": ["string"],
    "subflowId": "string|null",
    "artifactPath": "string|null",
    "inputArtifactPaths": ["string"],
    "outputArtifactPaths": ["string"],
    "inputMessageKeys": ["string"],
    "outputMessageKeys": ["string"],
    "outputSignalKeys": ["string"],
    "conditionExpression": "string|null",
    "loopExpression": "string|null",
    "approvalPrompt": "string|null",
    "modelPolicy": "PlatformModelPolicy|null"
  }
}
```

## 7. EdgeDraft
```json
{
  "id": "string",
  "source": "string",
  "target": "string",
  "label": "string|null",
  "branch": "default|true|false|loop|exit|null",
  "description": "string|null"
}
```

当前代码没有边级 `channelType` 字段；artifact/message/signal 由节点数据字段声明，control 由边和 `branch` 表达。若未来引入边级通道，必须先扩展 `PlatformFlowEdge` 并迁移 validator。

## 8. StageContract
```json
{
  "stageId": "string",
  "requiredArtifactIds": ["string"],
  "blockingIssueSeverities": ["blocker"],
  "confirmationPolicy": {
    "requireNoRunningCriticalTasks": true,
    "requireAllGuardsPass": true
  }
}
```

## 9. ArtifactDefinition
```json
{
  "id": "string",
  "name": "string",
  "kind": "markdown|table|image|diagram|mindmap|folder|openspec",
  "relativePath": "string",
  "format": "md|txt|pdf|csv|tsv|xlsx|png|jpg|svg|folder",
  "embedded": false
}
```

## 10. ExportMapping
```json
{
  "id": "string",
  "name": "string",
  "sourceArtifactIds": ["string"],
  "outputFormat": "md|txt|pdf|openspec|folder",
  "targetRelativePath": "string"
}
```

## 11. StageGuardStatus
```json
{
  "stageId": "string",
  "ready": false,
  "blockingReasons": [
    {
      "code": "missing_artifact|open_blocker|running_task|invalid_contract",
      "message": "string",
      "targetRef": "string|null"
    }
  ],
  "checkItems": [
    {
      "id": "string",
      "status": "pass|warn|block",
      "message": "string",
      "targetRef": "string|null"
    }
  ]
}
```

## 12. RuntimeRun / RuntimeEvent
```json
{
  "run": {
    "id": "string",
    "kind": "chat|stage|review|template|delivery",
    "status": "queued|running|pause-requested|paused|waiting-approval|merge-required|stopped|completed|failed",
    "createdAt": "string",
    "updatedAt": "string",
    "sessionId": "string|null",
    "stage": "AppStage|null",
    "roleId": "string|null",
    "flowId": "string|null",
    "selectedProfileId": "string|null",
    "contextPackId": "string|null",
    "evidencePackageId": "string|null",
    "outputs": ["RuntimeOutputRecord"],
    "artifactOutcomes": ["RuntimeArtifactOutcome"],
    "checkpoints": ["RuntimeCheckpoint"],
    "pendingApprovals": ["RuntimeApprovalRecord"],
    "recovery": "RuntimeRunRecovery|null",
    "controlState": "RuntimeRunControlState|null"
  },
  "events": [
    {
      "id": "string",
      "runId": "string",
      "createdAt": "string",
      "type": "run.started|run.resumed|model.selected|tool.requested|tool.completed|permission.blocked|checkpoint.saved|approval.waiting|approval.approved|approval.rejected|merge.required|subflow.started|subflow.completed|rerun.plan-created|run.recovery-saved|run.completed|run.failed",
      "message": "string",
      "metadata": {}
    }
  ]
}
```

## 13. 唯一性要求
- 另一套 AI 可以在实现语言层做适配，但不能删掉这些对象的核心字段类别。
- 特别禁止：
  - 把 `StageGuardStatus` 简化成单字符串
  - 把 `RuntimeRun` 和 `RuntimeEvent` 合并成一条日志文本
  - 把 `FlowDraft` 做成没有 `stageContracts/exportMappings` 的纯画布 JSON
