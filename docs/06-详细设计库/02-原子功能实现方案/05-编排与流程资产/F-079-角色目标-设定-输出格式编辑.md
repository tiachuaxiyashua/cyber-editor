# F-079 角色目标/设定/输出格式编辑

## 1. 基本信息
- 类型：用户可见功能
- 所属层级：编排与流程资产 / 角色 / 内容
- MSABC 分类：S
- 当前状态：部分完成
- 关联页面：[编排页](../../04-UI设计/07-编排页.md)
- 共享实现基线：[编排与流程资产](README.md)

## 2. 功能目标
- 角色要能定义系统设定、目标、输出格式和禁止事项。
- 分类解释：S：核心特色能力，决定 Cyber Editor 的差异化价值。
- 状态解释：已有部分代码或骨架，但交互、边界、稳定性或测试覆盖尚未收口。

## 3. 触发与前置条件
- 触发方式：选中角色编辑 Inspector
- 前置依赖：[INF-019 ModelRouter](../04-AI会话与阶段控制/INF-019-ModelRouter.md)、[INF-031 Flow asset persistence](INF-031-Flowassetpersistence.md)

## 4. 调用关系
- 上游入口：[编排页](../../04-UI设计/07-编排页.md)
- 下游调用：[INF-019 ModelRouter](../04-AI会话与阶段控制/INF-019-ModelRouter.md)、[INF-031 Flow asset persistence](INF-031-Flowassetpersistence.md)

## 5. 具体实现方案
### 前端/交互
- 在相关页面中提供符合层级的入口，避免把低频动作放到首屏噪音区。
- 该功能的 UI 状态必须覆盖：默认态、执行中、成功、失败、空状态。
- 本功能的页面与交互细节以对应 UI 页面文档和本分类 README 为准。

### 主进程/服务
- 由 `编排与流程资产` 分类共享模块承担核心实现，必要时通过 IPC 暴露给 renderer。
- 任何真实读写、运行、导出、安装、诊断都必须在主进程或服务层完成，不允许仅在 renderer 假装成功。

### 数据与持久化
- 该功能涉及的数据结构应写入本分类 README 中列出的共享数据结构，不要另造一套旁路状态。
- 如果该功能会改变工程、模板、Flow 或设置，必须有稳定的持久化与恢复路径。

### 校验与异常
- 提交动作前校验输入、依赖、权限与边界。
- 失败时保留用户上下文，不得悄悄吞掉错误或重置已有布局。
- 当前已有部分代码，但必须优先补完缺失的 guard、状态反馈、稳定性和测试。

## 6. 自动测试
- 单元：验证该功能对应的核心服务函数、校验器或状态机。
- 集成/端到端：按用户入口触发该功能，断言 UI 反馈、数据落盘和下游状态变化。
- 回归：覆盖窗口缩放、主题切换、重新打开工程/Flow、失败重试等边界。

## 7. 用户模拟测试
1. 以小白用户视角走一遍最短主路径，确认是否能在首屏找到入口。
2. 以熟练用户视角验证对象级高频动作、右键菜单、命令面板和深层高级入口。
3. 如果该功能依赖 AI、模板或编排运行，再验证失败、重试、恢复和保存后的二次打开。

## 8. 当前审计结论
- 审计状态：部分完成
- 审计说明：已有部分代码或骨架，但交互、边界、稳定性或测试覆盖尚未收口。
